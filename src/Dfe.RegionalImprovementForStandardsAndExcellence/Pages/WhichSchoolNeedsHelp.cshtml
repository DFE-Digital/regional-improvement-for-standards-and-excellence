@page "/which-school-needs-help"
@using Dfe.RegionalImprovementForStandardsAndExcellence.Pages.Shared
@using Dfe.RegionalImprovementForStandardsAndExcellence.Models
@using Dfe.RegionalImprovementForStandardsAndExcellence.Extensions
@model Dfe.RegionalImprovementForStandardsAndExcellence.Pages.WhichSchoolNeedsHelpModel

@{
    Layout = "_Layout";
    ViewBag.Title = (!ViewData.ModelState.IsValid ? "Error: " : "") + "Which school is involved";
    var routeParams = new Dictionary<string, string>
    {
        { "urn", Request.Query["urn"] },
        { "ukprn", Request.Query["ukprn"] }
    };
}

@section BeforeMain
{
    <partial name="_BackLink" model="@(new BackLink(Links.NewProject.NewConversionInformation.Page, routeParams))" />
    <partial name="_ErrorSummary" />
}

<div class="govuk-grid-row">
<div class="govuk-grid-column-two-thirds">
    <form method="post" data-id="form">
        <div class="govuk-form-group @ModelState.GetErrorStyleClass()">
            <h1 class="govuk-label-wrapper">
                <label class="govuk-label govuk-label--xl" for="SearchQuery">
                    Which school needs help?
                </label>
            </h1>
            <partial name="_AutoComplete" model="@Model.AutoCompleteSearchModel" />
        </div>
        <button class="govuk-button" type="submit" data-id="submit">Continue</button>
    </form>
</div>
</div>
